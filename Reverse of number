#include <iostream>
#include <string>
using namespace std;
void reverse(string a){
	int length=a.length();
	cout<<"Reversed number is= ";
	for(int i=length-1; i>=0; --i){
		cout<<a[i];
	}
}
int main(){
	string a;
	cout<<"Enter any number= ";
	cin>>a;
	reverse(a);
}
