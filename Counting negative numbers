#include <iostream>
using namespace std;
int main(){
	int number, count=0;
	cout<<"Enter 10 numbers"<<endl;
	for(int i=0; i<=10; ++i)
	{
		cout<<"Enter a number= ";
		cin>>number;
		if(number<0)
		{
			count++;
		}
	}cout<<"You have entered "<<count<<" negative numbers. ";
}
